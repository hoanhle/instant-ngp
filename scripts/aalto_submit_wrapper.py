from render


#----------------------------------------------------------------------------
# Must be in separate file, since __main__ is overridden.

def run_training(submit_config: dict, **kwargs) -> None:
    """Runs EDM2 model training."""